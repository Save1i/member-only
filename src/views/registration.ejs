<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>sign-up</title>
</head>
<body>
    <a href="/message">message</a>
    <form action="sign-up" method="post">
        <label for="firstName">First name:</label>
        <input type="text" name="firstName" id="firstName" value="<%= info ? info.firstName : '' %>" >
        <label for="lastName">Last name:</label>
        <input type="text" name="lastName" id="lastName" value="<%= info ? info.lastName : '' %>">
        <label for="nickName">Nick name:</label>
        <input type="text" name="nickName" id="nickName" value="<%= info ? info.nickName : '' %>">
        <label for="password">Password:</label>
        <input type="text" name="password" id="password" value="<%= info ? info.password : '' %>">
        <button type="submit">create</button>
</form>
    <% if(warning){ %>
        <% warning.forEach(error => { %>
            <p><%= error.msg %></p>
        <% }); %>
        <% } %>
</body>
</html>
